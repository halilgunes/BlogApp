@model IEnumerable<BlogGroup>

@if (Model.Count() == 0)
{
    <div class="alert alert-warning">Kayıt yok..</div>
}
else
{

@*    @foreach (var item in Model)
    {
        <div class="card mb-2">
            <div class="card-header">
                @item.Title
            </div>
            <img class="card-img-top" src="~/img/@item.Image" alt="Card image cap">

            <div class="card-block">
                <h4 class="card-title">  @item.Title</h4>
                <p class="card-text">  @item.Description</p>
                <a asp-action="Details" asp-controller="Blog" asp-route-id="@item.BlogId" class="btn btn-primary">Blog Details</a>
            </div>
        </div>
    }
*@
    <div class="container-fluid">

        @foreach (var item in Model)
        {
            <div class="card-group">
                @foreach (var blogItem in item.BlogList)
                {
                    <div class="card text-center w-50 h-100">
                        <img class="card-img-top w-100 h-50" src="~/img/@blogItem.Image" alt="Card image cap">
                        <div class="card-body">
                            <h5 class="card-title">@blogItem.Title</h5>
                            <p class="card-text m-2">@blogItem.Description</p>
                            <a asp-action="Details" asp-controller="Blog" asp-route-id="@blogItem.BlogId" class="btn btn-secondary">Blog Details</a>
                        </div>
                        <div class="card-footer">
                            <small class="text-muted">Last updated 3 mins ago</small>
                        </div>
                    </div>
                }
            </div>
        }
    </div>


}